env:
   WORKDIR: "/t/b"
   CIRRUS_CLONE_DEPTH: "1"
task:
  name: "bui"
  timeout_in: 2h
  container:
        image: apon77/aosp:cirrus
        cpu: 8
        memory: 32G
   
  Check_background_script:
     - cat /etc/os* && env && nproc && gcc --version && clang --version
  
  S-ync_script:
     - cd $CIRRUS_WORKING_DIR
     - chmod u+r+x s.sh
     - ./ziping.sh
